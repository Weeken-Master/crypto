/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['down-vn.img.susercontent.com','web.telegram.org','pbs.twimg.com'],
      },

    
};

export default nextConfig;
